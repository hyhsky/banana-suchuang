快速开始 - 3步完成部署
第一步：安装依赖 (1分钟)
在项目目录下打开终端，运行：

npm install
等待安装完成。

第二步：推送代码到 GitHub (2分钟)
如果你的项目还没有推送到 GitHub：
初始化 Git（如果还没有）
git init
添加所有文件
git add .
提交
git commit -m "添加图片上传后端服务"
在 GitHub 创建新仓库

访问 https://github.com/new
仓库名随便起，例如 banana-suchuang
选择 Public 或 Private（都可以）
不要勾选 "Add a README file"
点击 "Create repository"
关联并推送

# 将下面的 YOUR_USERNAME 替换为你的 GitHub 用户名
git remote add origin https://github.com/YOUR_USERNAME/banana-suchuang.git
git branch -M main
git push -u origin main
如果项目已经在 GitHub 上：
git add .
git commit -m "添加图片上传后端服务"
git push
第三步：部署到 Vercel (2分钟)
打开 Vercel 访问 https://vercel.com

登录/注册 点击右上角 "Sign Up" 或 "Login" 选择 "Continue with GitHub"

导入项目

登录后，点击 "Add New..." → "Project"
你会看到你的 GitHub 仓库列表
找到 banana-suchuang（或你的仓库名）
点击旁边的 "Import"
配置项目（自动识别，无需修改）

Framework Preset: Vite ✅ (自动识别)
Root Directory: ./ ✅
Build Command: npm run build ✅
Output Directory: dist ✅
直接点击底部的 "Deploy" 按钮

等待部署完成

进度条跑完大约需要 1-2 分钟
看到 "🎉 Your project has been successfully deployed" 就成功了
访问你的应用

Vercel 会给你一个域名，例如：https://banana-suchuang.vercel.app
点击 "Visit" 或直接访问这个网址
测试图生图功能 🎨
✅ 完成！
现在你的应用已经部署到全球 CDN，可以正常使用图生图功能了！

如果遇到问题：
问题1：上传图片后还是报错

检查浏览器控制台（F12）的错误信息
确认 Vercel 部署成功（状态显示 "Ready"）
问题2：Cannot find module 'form-data'

回到项目目录运行 npm install
重新 git push
Vercel 会自动重新部署
问题3：GitHub push 失败

确认你已经登录 GitHub
检查 remote 地址是否正确：git remote -v
🎯 下次更新代码后怎么办？
非常简单：

git add .
git commit -m "更新描述"
git push
Vercel 会自动检测到代码变化并重新部署，无需手动操作！